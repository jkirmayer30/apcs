1. Start at the root
2. If the value is greater than the root, make the root its left child
3. If the value to insert is between this node and its left child, make
the node's left child the value's left child and make the value the node's
new left child.
4. If the current node has no left children add the value as its' left child
5. If none of this is true repeat steps 2 and 3 on the nodes left child
